// This file ensures the page is rendered on-demand rather than at build time
// The not-found.js file can be used in addition to route-config.js for the App Router

export default function NotFound() {
  // This is just a placeholder - in most cases, it shouldn't be rendered
  return null;
} 