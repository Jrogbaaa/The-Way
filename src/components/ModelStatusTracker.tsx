'use client';

// This is a stub component that replaces the removed ModelStatusTracker
// It's kept to avoid breaking imports in case there are still references to it

export default function ModelStatusTracker() {
  // Return nothing since this feature has been removed
  return null;
} 